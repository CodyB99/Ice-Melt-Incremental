# Architecture and Product Decisions

Record decisions that materially change scope, dependencies, data schema, security, economy, or release behavior.

| Date | Decision | Reason | Consequences |
|---|---|---|---|
| 2026-08-02 | Use a small three-zone MVP | Launch and validate quickly | Later content must remain configuration-driven |
| 2026-08-02 | Use vanilla Roblox APIs for MVP | Reduce dependency and setup risk | Persistence/session safety must be tested carefully |
| 2026-08-02 | Server-authoritative melt rewards | Prevent simple remote exploitation | Client effects may be predicted but reconciled |
| 2026-08-02 | One Thaw prestige layer | Avoid progression bloat before retention data | Additional layers remain post-launch backlog |
| 2026-08-02 | Environment resolved once in `EnvironmentConfig`, never from `RunService:IsStudio()` at call sites | Phase 00 requires the mode to be explicit and production to be unreachable by accident | Production requires an allow-listed PlaceId and is rejected outright in Studio; unregistered places fall back to Test, never Production |
| 2026-08-02 | World skeleton built at runtime by the server, not hand-placed in Studio | Reproducible from source control, cannot drift from configuration, and matches the phase requirement for server-created runtime folders | Edit mode shows a bare place; the world only exists in Play. Builder is idempotent and versioned so layout changes rebuild rather than accumulate |
| 2026-08-02 | Foreign SpawnLocations disabled rather than destroyed at startup | The Baseplate template spawn sits inside the Backyard slab and would break onboarding | Change is reversible and logged; only `Hub/Spawn` is enabled |
| 2026-08-02 | Commit selene's generated `roblox.yml` instead of downloading it per machine | Norton 360's HTTPS interception makes selene's API-dump download fail with `UnknownIssuer`; rustls ignores the Windows certificate store | `selene src` runs offline everywhere. Regenerate only on a material Roblox API change, which needs Norton's web scanning paused once |
| 2026-08-02 | Run both `luau-lsp analyze` and `selene` as required checks | They catch different classes of defect: the typechecker found a union-narrowing bug, selene found duplicate `if`/`else` branches | `scripts/analyze.ps1` and `scripts/lint.ps1` both gate a phase, and both fail hard rather than warn |
| 2026-08-02 | Rojo serves on port 34873 | The default 34872 is permanently held by another project on this machine | Port pinned in `default.project.json`; the Rojo plugin must be set to 34873 or it will sync the wrong project |
| 2026-08-02 | `.gitattributes` pins `* text=auto eol=crlf` | `core.autocrlf` was guessing per file, so `.luau` was CRLF and everything else LF | Repository stores LF, working tree is CRLF, matching `stylua.toml` and PowerShell. All 46 mixed files normalised |
| 2026-08-02 | No client-invoked RemoteFunctions; all 14 remotes are RemoteEvents | A RemoteFunction invoked from a client parks a server thread until it returns, so a malicious client can stall server work just by calling one | Requests are fire-and-forget events; the server answers on `ActionResult` or `PlayerState`. Any future request/response pair must follow the same shape |
| 2026-08-02 | Remote schema is a closed list with enforced direction | "Server creates only expected remotes" has to be checkable, not a convention | `RemoteRegistry` destroys anything in the Remotes folder not in the schema, and both sides refuse to use a remote against its declared direction |
| 2026-08-02 | Added `ClientReady` and `ActionResult` beyond the kit's original remote list | The phase requires a safe handshake and a state-ready signal, and denials need a channel back to the client | A generic server→client *result* channel is safe; a generic client→server *command* channel is not, and `ConfigAudit` fails if one is ever added |
| 2026-08-02 | `Init` must not yield; detected rather than documented | A yield in `Init` silently reorders startup and reintroduces the race the two-phase lifecycle exists to remove | `Lifecycle` runs `Init` in a coroutine and reports any entry left suspended. Work that must wait belongs in `Start` |
| 2026-08-02 | Client handshake retries a bounded 4 times | `ClientReady` is an event, so firing it before the server connects its listener drops it silently and leaves the client with no state forever | Retry stops on first snapshot and warns if it gives up. The server treats `ClientReady` as idempotent and rate-limits it |
| 2026-08-02 | Union-typed config values go through typed constructors, not table literals | A string literal in a table widens to `string` and loses the union check, which `luau-lsp` rejected | `discovery()` in ContentConfig and `spec()` in RemoteNames; a typo like `"Mythic"` now fails to typecheck at the call site |
| 2026-08-02 | Every profile read and write goes through `UpdateAsync`, never Get/SetAsync | Only UpdateAsync makes the session-lock check and the write it guards a single transaction; with Get then Set, two servers can interleave and one silently wins | All storage goes through one `update(key, transform)` primitive. A transform returning nil aborts the write, which is how a lock conflict declines rather than clobbers |
| 2026-08-02 | A profile that fails to load results in a kick, never in default data | Handing out defaults and then autosaving writes them over a profile that is probably fine. A kick is recoverable; overwritten save data is not | Distinct messages for `Locked`, `Incompatible`, and `Failed`. The player is told their progress is safe |
| 2026-08-02 | A profile from a newer schema version aborts the load entirely | Refusing to migrate is not enough on its own: repair would still strip unknown fields and the next save would persist the stripped copy | `normalise` reports `tooNew` and `DataService` returns `Incompatible` without claiming the lock. Verified: version, Heat, and an unknown field all survived |
| 2026-08-02 | Studio session-lock identity is stable per place | `game.JobId` is empty in Studio, so a time-derived id made every play session look like a different server and locked the developer out of their own profile for the 150s timeout | A restarted Studio session reclaims its own lock immediately. Safe because Studio cannot reach production and profiles are keyed per user |
| 2026-08-02 | Session lock expires after 150s rather than being permanent | A crashed server never releases its lock; without expiry the player could never rejoin | Chosen to be well above the 30s heartbeat so a healthy server never looks stale, and low enough that a crash costs one short wait |
| 2026-08-02 | Client-facing state is an explicit allow-list, not a filtered profile | Filtering means remembering to strip each new secret field; an allow-list means a new field is private until someone deliberately publishes it | `buildSnapshot` names all 8 fields. Verified on the wire that ReceiptHistory, LifetimeHeat, RunHeat, RewardState, Settings, and Discoveries never leave the server |
| 2026-08-02 | Dev commands are a ServerStorage BindableFunction, not a remote | ServerStorage does not replicate, so the entry point is invisible to clients. A client-callable admin channel is exactly what the Phase 01 remote schema forbids | Reachable from the Studio command bar only, and not created at all when the environment is Production |
| 2026-08-02 | Studio falls back to a non-persistent in-memory store when API access is off, but a live server never does | Development must not be blocked by a Studio setting, but silently "saving" to memory on a live server would destroy real progress | The fallback warns loudly and `DataService.isPersistent()` reports it |
| 2026-08-02 | The melt payload carries only a sequence number | The server derives position, zone, stats, targets, damage, and reward from authoritative state. Validating a client-supplied target list costs more than computing the correct one, and accepting one means trusting a client to name its own targets | There is nothing in the payload to forge. Position comes from the server's copy of the HumanoidRootPart |
| 2026-08-02 | Cell parts are created once and never destroyed | Melting hides a cell and respawn restores it, so sustained play allocates nothing. Create/destroy per melt is the churn docs/02 forbids | Verified: 256 instances before, during, and after a heavy melt and respawn cycle |
| 2026-08-02 | Cells are found by grid index arithmetic, not by scanning | A regular grid means the cells inside a radius can be computed directly, so cost scales with radius rather than field size | Field dimensions must stay a regular grid. Irregular set pieces in Phase 05 need their own lookup rather than being mixed into this one |
| 2026-08-02 | Cell appearance updates only on a crack-stage boundary | Property writes replicate. Updating on every point of damage would flood the network to show a change no one can see | Four visible stages per cell |
| 2026-08-02 | Crits multiply reward, not damage | Scaling both would make Crit a second Power stat and double-dip against the balance curve in docs/03 | Crit is purely an economy stat |
| 2026-08-02 | The client suppresses pulses when far from every ice field | Ten remotes a second per player, forever, for guaranteed rejections is real waste at scale | Explicitly a bandwidth optimisation, not a security control. The server validates every pulse regardless, and the margin is the maximum possible radius so it can never suppress a pulse that would have succeeded |
| 2026-08-02 | Melt rejection reasons are echoed to the client in development only, throttled per reason | On a live server, telling a client exactly which validation it failed is free tuning information for an exploit. Unthrottled, a persistent condition buries the Output | One echo per reason per second, and none at all in Production |
| 2026-08-02 | Melt damage is scaled by elapsed time, not applied per pulse | `docs/03` specifies Power as damage per second. Applying it per pulse made it 10x stronger than specified and let a faster-pulsing client deal proportionally more damage | The melt rate no longer depends on pulse frequency, so the rate limiter protects bandwidth rather than being the only thing between a fast client and free progress. The delta is clamped so a resumed player cannot land one accumulated hit |
| 2026-08-02 | Derived stats are computed in one server module and replicated as finished numbers | Upgrades, tools, the HUD, and walk speed all need the same figures. Four copies of "what is this player's radius" is four places to forget a future pass or cap | `StatsService` caches per player and zone and is invalidated on any change. The client displays what the server decided; only the unbought "after" preview is computed locally, from the same shared config |
| 2026-08-02 | Purchase requests name a thing, never a price or a quantity | The server reads the current level from the profile and derives the cost from shared configuration, so there is no number in the message to tamper with. "Cannot buy free upgrades" is structural rather than a check that could be forgotten | Verified: a forged `Cost = 0` was charged full price. Tool unlock takes no index at all; it always means "the next one" |
| 2026-08-02 | Spending goes through one atomic `trySpend` | Checking a balance, doing other work, then deducting leaves a window where two requests both pass the check. Making the atomic form the only convenient option removes the chance to write that bug | `EconomyService` also gives Phase 10 analytics a single place to hook every source and sink |
| 2026-08-02 | A failed grant after a successful charge refunds | Reaching that branch means a bug, but a bug that charges a player and gives nothing is the worst kind | Both `UpgradeService` and `ToolService` refund and warn rather than silently pocketing Heat |
| 2026-08-02 | Ice field pitch is a tuning lever; documented economy values are not | Pitch is a choice made in `IceFieldConfig`, whereas Power's cost of 15 and Backyard's durability of 10 come from `docs/03`. A pacing problem created by the field layout should be fixed in the field layout | Pitch measured across 6, 7, 8, and 9 studs and kept at 6. The remaining Candle pacing gap is left as an owner decision rather than silently rebalancing documented values on simulated data |
| 2026-08-02 | Locked zones are guarded twice: by the travel remote and by a position patrol | The two fail differently. The remote guard cannot see an exploiter who moves their own character, because that player never sends a request | The patrol returns anyone standing in a zone they do not own. MeltService independently refuses to award Heat outside an unlocked zone, so the window before the patrol fires earns nothing either |
| 2026-08-02 | Zone membership is defined once, in `ZoneService.zoneAt` | MeltService previously had its own copy of the rectangle test. Two definitions could drift, and a player could then be in-zone for melting but out-of-zone for enforcement | Every caller asks the same function. Travel sets position; it never sets a zone the server then trusts |
| 2026-08-02 | Zones unlock strictly in order | Without a ladder check a player could buy the Frozen City directly from the Backyard, skipping the Cavern and landing where the balance curve never intended | Verified: buying City with Cavern locked is denied `PreviousZoneLocked` |
| 2026-08-02 | Zone lighting is applied client-side | Lighting is one global object, so setting it on the server would give every player the look of whichever zone someone entered last | `ZoneController` tweens per client, which is the only correct behaviour once players can be in different zones at once |
| 2026-08-02 | Zone props are seeded, not random | An unseeded generator would make two servers visually different and a bug report impossible to reproduce | `ZoneStyleConfig.PlacementSeed`, offset per zone so the three do not share a layout |
| 2026-08-02 | The documented economy does not compound; left as an owner decision | Every upgrade in docs/03 has cost growth exceeding effect growth, so payback lengthens with each level and income decelerates. Zones do not compensate: Heat per durability is 0.10, 0.12, 0.15 across the three | Recorded with the arithmetic in PROJECT_STATUS rather than silently rebalancing documented values on simulated data. Blocks meaningful tuning of Phases 06 and 07 |
| 2026-08-02 | The Thaw confirmation token is an intent nonce, not a promised award | A token carrying an amount could be confirmed later against a run that has moved on, locking in a payout the player no longer deserves | The award is recomputed at confirmation from live state and eligibility re-checked. The token is spent before any state changes, so a doubled confirmation finds nothing pending |
| 2026-08-02 | Thaw tokens are invalidated on profile load | A token captured before a rejoin describes a profile that no longer exists | Verified: a pre-rejoin token was denied with no Embers granted and no Thaw counted |
| 2026-08-02 | `applyThawReset` writes every field explicitly rather than clearing and rebuilding | A profile field added in a later phase is then retained by default. Silently wiping something new because a reset was too clever is how prestige systems destroy progress | LifetimeHeat is deliberately retained: it is a lifetime counter, and resetting it would make it a run counter under a misleading name |
| 2026-08-02 | Thaw saves immediately rather than waiting for autosave | A Thaw is the worst thing to lose to a crash, because the run that paid for it is already gone | One extra write per Thaw, which is rare by design |
| 2026-08-02 | No permanent Ember node set was built | `docs/03` lists "permanent Ember nodes" among retained fields but never specifies any, and Phase 06 says to add them "if documented" | Inventing an undocumented progression system would be scope invention. Revisit when the nodes are specified |
| 2026-08-02 | `AnalyticsService` added now as a thin sink | The events Phase 06 must log need somewhere to go that is not scattered prints, and Phase 10 can then attach the real backend in one function | Server-emitted and low-cardinality from the start, per docs/08, since both are expensive to retrofit |
| 2026-08-02 | First published place registered as **Test**, not Production | Publishing must never silently point a live server at production player data | `PRODUCTION_PLACE_IDS` stays empty until the owner explicitly promotes a place |
| 2026-08-02 | Auto-Melt implemented as server-driven extra pulses, not removed | `docs/01` requires Auto-Melt to be earnable through progression and `docs/03` gives it a full cost/effect curve, so deleting the track would have broken the documented seven-track MVP scope. It was sold, charged, capped and displayed while `AutoRate` had no consumer anywhere | A per-player sweep in `MeltService` runs `AutoRate` pulses/sec through the same `applyMelt` path as a player pulse, so both share one cell query, one cap, and one credit rule. Each auto pulse deals a fixed nominal pulse rather than elapsed-time-scaled damage, because the manual delta already bills every second of wall clock and scaling both would pay for the same second twice |
| 2026-08-02 | Melt damage and the reward path extracted into one `applyMelt` | Auto-Melt needed the identical validation, cap, and one-credit-per-cell rule. A second implementation would have been a second place to forget a clamp | Player pulses and Auto-Melt differ only in how the damage figure is derived and in the two guards that exist purely to police a client (sequence, rate limit) |
| 2026-08-02 | Stat-cache invalidation moved into `PlayerStateService`, not its callers | It lived only in `UpgradeService.purchase`, so every other route into a stat-changing mutation — dev commands, a Thaw, anything a later phase adds — left `StatsService` serving the block from before the change | A `commit` helper marks dirty, invalidates, and queues a push. Deliberately **not** used by `addHeat`, which runs up to twelve times a second per player and cannot affect stats; invalidating there would defeat the cache the service exists to provide |
| 2026-08-02 | A Thaw re-applies stats to the live character | A Thaw is the only path that *lowers* stats. Walk speed is otherwise written only on purchase and on spawn, so the character kept the speed of the run it just spent, and re-buying the first Speed level would have *slowed the player down* — a reset that reads as a broken upgrade | `ThawService.confirm` calls `applyWalkSpeed` and `refreshHeldModel`. Verified on a character that was never respawned: 31 → 16 studs/s, held model Blowtorch → Match |
| 2026-08-02 | Writes to one profile key are serialised per player | `saveProfile` yields inside `UpdateAsync`, so an autosave and a release could be in flight on the same key at once and land in either order. If the autosave landed second it rewrote the Lock the release had just cleared, with a fresh heartbeat, locking the player out of their own save for the full 150s on rejoin | An in-flight flag with a bounded wait, plus a re-check that declines a non-release write once a release has been requested. The flag is cleared through a `pcall` so a throwing write cannot leak it and silently stop saving that player |
| 2026-08-02 | HUD panel height is relative with a maximum, not a fixed 340px | A fixed 340px panel whose bottom sits 84px up needs 424px of height; a phone in landscape has ~354px. The top of every panel rendered above the screen edge, and a `ScrollingFrame` cannot scroll above its own canvas origin, so that content was unreachable rather than merely off-screen | `UDim2.new(0, 320, 1, -96)` with a `UISizeConstraint` MaxSize. Identical geometry on any viewport at or above 504px, so desktop is unchanged |
| 2026-08-02 | The recorded "first Thaw takes ~20 days" figure was wrong and is corrected | It extrapolated the measured 0.58 Heat/sec as a constant, ignoring that the player buys upgrades. Simulated against the real `StatMath` and configuration, a player who spends earns the first Thaw in roughly 1.8 hours | The economy is slow against the documented targets, not broken by four orders of magnitude. The root cause recorded under Phases 04–06 still stands: no track has effect growth at or above cost growth. Still an owner decision, still needs a human playtest |
